{
  "name": "lv-editor",
  "version": "0.1.0",
  "scripts": {
    "build:main": "tsc --p src/main/tsconfig.json",
    "start:electron": "electron ./build/main/main.js",
    "dev": "npm-run-all clear build:* start:*",
    "prod": "echo prod"
  },
  "dependencies": {
    "docker-modem": "^2.1.1",
    "dockerode-ts": "^0.3.2",
    "electron": "^5.0.4",
    "node-docker-api": "^1.1.22"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
